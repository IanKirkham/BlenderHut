import 'package:flutter/material.dart';

class RecipeBuilder extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Recipe Builder Page"),
      ),
    );
  }
}
